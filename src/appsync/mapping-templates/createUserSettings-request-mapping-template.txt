{
    "version" : "2017-02-28",
    "operation" : "PutItem",
    "key" : {
        "id": $util.dynamodb.toDynamoDBJson($ctx.identity.cognitoIdentityId),
    },
    #set( $input = $util.dynamodb.toMapValues($ctx.args.input) )
    #set( $input.createDate = $util.dynamodb.toString($util.time.nowISO8601()) )
    #set( $input.lastUpdated = $util.dynamodb.toString($util.time.nowISO8601()) )

    "attributeValues" : $util.toJson($input)
}